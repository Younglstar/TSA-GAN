"""Project-wide helpers (I/O, logging, small utilities).

This folder is intentionally lightweight: it centralizes tiny pieces of
repeated logic (pickle I/O, logging setup, etc.) so the pipeline scripts
stay short and readable.
"""
